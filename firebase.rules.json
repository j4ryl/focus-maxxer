{
  "rules": {
    "demo": {
      "activeVideo": {
        ".read": true,
        ".write": true,
        ".validate": "newData.isString() && newData.val().matches(/^(gta|subway|sand|family)$/)"
      }
    }
  }
}
